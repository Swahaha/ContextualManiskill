from setuptools import setup, find_packages

setup(
    name="contextual_maniskill",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        "mani_skill",
        # Add other dependencies as needed
    ],
    python_requires=">=3.9",
)
